<%- include("partials/header") %>

<h1><%= homeContentTitle %></h1>
<p><%= homeContentBody %></p>

<div> 
     
    <% submittedPost.forEach(function (post){ %>
        <div>
            <h3><%= post.blogTitle %></h3>
            <p><%= post.blogBody.substring(0, 100); %> ... <a href="/posts/<%= post.blogTitle %>" >Read More</a> </p>
        </div>
    <% }) %>

</div>

<%- include("partials/footer") %>